# libpromeki Development Plan

## Overview

This plan builds out the consolidated `promeki` library (core, network, proav, music) and the separate `promeki-tui` and `promeki-sdl` UI libraries toward completeness. Work is organized into phases ordered by dependency.

**Maintenance note:** Completed items are removed from individual phase documents once merged, unless they provide context needed by pending work. The code and git history are the source of truth for completed work.

## Current Focus

All media work happens in the `MediaIO` framework and its backends. The legacy `MediaNode` / `MediaPipeline` / `*Node` layer and its two dedicated utilities (`vidgen`, `testrender`) have been deleted; the tree is clean. The next major milestone is a new `MediaPipeline` class that builds on MediaIO to describe and run full pipelines from a data object (or JSON).

**Primary work queue (in rough order):**

1. **MediaIOTask_Rtp** (see `proav_nodes.md`):
   - Writer path is SHIPPED — unified video + audio + metadata sink with MJPEG / L16 / JSON / JPEG XS payloads, SDP export, kernel pacing, per-stream TX worker threads, and timing histograms.
   - **Reader path is SHIPPED** — per-stream RTP receive threads, packet reassembly, SDP-driven auto-config (`RtpSdp` config key), video/audio/data frame aggregation, jitter buffer, and comprehensive loopback tests.
   - Remaining reader work: mid-stream descriptor discovery, RTP timestamp wrap handling.
2. **New `MediaPipeline` class** (see `proav_pipeline.md`):
   - `MediaPipelineConfig` data object describing stages + routes, with JSON `toJson()`/`fromJson()`.
   - `MediaPipeline` class that builds, opens, starts, stops, and closes a pipeline of MediaIO instances.
3. **mediaplay JSON pipeline support** (see `proav_nodes.md` → mediaplay section):
   - `--pipeline <file.json>` / `--save-pipeline` — blocked on new `MediaPipelineConfig`.
4. **JPEG XS container and RTP follow-ups** (see `fixme.md`):
   - QuickTime / ISO-BMFF `jxsm` sample entry (read + write) per ISO/IEC 21122-3 Annex C — **blocked on procuring the spec**.
   - `RtpPayloadJpegXs` slice packetization mode (K=1) for ST 2110-22.
   - Packed RGB encode path in the codec.
5. **Network optimization follow-ups** (see `proav_optimization.md`):
   - Core prerequisites SHIPPED: `UdpSocket::setPacingRate()`, `UdpSocket::writeDatagrams()` (sendmmsg batch send), `PacketTransport` abstraction with `UdpSocketTransport` and `LoopbackTransport`, `RtpSession` migrated to `PacketTransport`, `SO_TXTIME` enable path.
   - Remaining: wire `RtpPacingMode::TxTime` through `MediaIOTask_Rtp` so per-packet `SCM_TXTIME` deadlines actually get stamped onto each datagram.  DPDK transport backend as a later phase.

## Documents

| Document | Phase | Description |
|---|---|---|
| [core_utilities.md](core_utilities.md) | 7 | Enhanced existing classes (Variant, String, RegEx, Result adoption) |
| [core_io.md](core_io.md) | 2 | IODevice, FilePath, Dir, Process (COMPLETE; carries known issues) |
| [core_streams.md](core_streams.md) | 2, 7 | DataStream, TextStream extensions, ObjectBase saveState/loadState |
| [network_sockets.md](network_sockets.md) | 3A | Socket layer (Abstract, TCP, UDP, Raw, TLS) — COMPLETE |
| [network_protocols.md](network_protocols.md) | 3B | HTTP, WebSocket, higher-level protocols |
| [network_avoverip.md](network_avoverip.md) | 3C | AV-over-IP building blocks (RTP, PTP, SDP, multicast) |
| [proav_pipeline.md](proav_pipeline.md) | 4A | `MediaPipeline` class (MediaIO-based, JSON-definable) |
| [proav_nodes.md](proav_nodes.md) | 4B | MediaIO backends (all complete: TPG, ImageFile, AudioFile, QuickTime, Converter, Rtp, SDL) |
| [nvenc.md](nvenc.md) | 4B | NVENC hardware H.264 / HEVC / AV1 encoder (functional test green; HDR + multi-format landed) |
| [quicktime.md](quicktime.md) | 4B | QuickTime writer investigation and landed spec-cleanliness fixes; drain-at-close deferred |
| [proav_dsp.md](proav_dsp.md) | 4C | DSP and effects (future, as Converter subclasses) |
| [proav_optimization.md](proav_optimization.md) | 4D | Network optimization (sendmmsg, kernel pacing, PacketTransport) |
| [proav_timestamps.md](proav_timestamps.md) | 4E | MediaTimeStamp, ClockDomain, EUI64, PTP/SDP integration, auto-stamping |
| [proav_framesync.md](proav_framesync.md) | 4F | Clock / FrameSync / SDLPlayer (phases 1–3 shipped; FramePacer removal pending) |
| [tui.md](tui.md) | 5 | TUI widget completion |
| [music_theory.md](music_theory.md) | 6A, 6B | Core music theory objects |
| [music_midi.md](music_midi.md) | 6C, 6D | MIDI I/O and arrangement |
| [logger_ring_buffer.md](logger_ring_buffer.md) | cross-cutting | Retain last N log messages in a lock-free ring for inclusion in crash reports |
| [benchmarking.md](benchmarking.md) | cross-cutting | `BenchmarkRunner`, `promeki-bench` utility, MediaIO stamp hooks, live telemetry |
| [fixme.md](fixme.md) | ongoing | Existing FIXME comments tracked across the tree |
| [ideas.md](ideas.md) | backlog | Exploratory ideas that need further design |

## Dependency Graph

```
Phase 1 (COMPLETE) ──┬─► Phase 2 (COMPLETE)
                     │     │
                     │     ├─► Phase 3A (COMPLETE) ─► Phase 3C (mostly complete)
                     │     │
                     │     ├─► Phase 4 (MediaIO framework + backends complete)
                     │     │     │
                     │     │     ├─► Phase 4A: MediaPipeline (MediaIO-based)
                     │     │     │
                     │     │     └─► mediaplay --pipeline JSON support
                     │     │
                     │     ├─► Phase 4D (network optimization)
                     │     │     │
                     │     │     └─► PacketTransport ◄─┐
                     │     │                           │
                     │     │     Phase 3B (HTTP/TLS) ──┘
                     │     │
                     │     └─► Phase 7 (ObjectBase saveState/loadState via DataStream)
                     │
                     ├─► Phase 5 (TUI Widgets)          [mostly independent]
                     │
                     └─► Phase 6 (Music Library)        [mostly independent]

Phase 7 (Cross-Cutting) — ongoing throughout
```

## Phasing

### Phase 1: Core Containers, Concurrency, Utilities — COMPLETE
Delivered: List, Map, Set, HashMap, HashSet, Deque, Stack, PriorityQueue, Span, Mutex, ReadWriteLock, WaitCondition, Atomic, Future, Promise, ThreadPool, Queue, Random, ElapsedTimer, Duration, Algorithm, Result, Pair.

### Phase 2: IO Abstractions, Filesystem, Streams — COMPLETE
IO abstractions, filesystem utilities, DataStream, and TextStream all implemented. Resource filesystem (cirf) integrated: `File`, `FileIODevice`, and `Dir` transparently accept `:/` resource paths. libpromeki ships `:/.PROMEKI/` built-in resources (bundled FiraCode font). Remaining extension work lives in `core_streams.md` (TextStream type operators, ObjectBase serialization).

### Phase 3: Network Library — IN PROGRESS
**Phase 3A (Sockets) COMPLETE** — including `UdpSocket::writeDatagrams()` (sendmmsg), `setPacingRate()`, and `setTxTime()`. Phase 3B (HTTP/TLS) not started. **Phase 3C (AV-over-IP) mostly complete** — PrioritySocket, `PacketTransport` + `UdpSocketTransport` + `LoopbackTransport`, RtpSession (migrated to PacketTransport, incl. `sendPacketsPaced()`, `setRemote()`, `setPacingRate()`, plus new `startReceiving()` / `stopReceiving()` receive loop), RtpPacket, RtpPayload (L24, L16, RawVideo, JPEG with RFC 2435 DQT/entropy parsing, `RtpPayloadJson` for metadata streams, **`RtpPayloadJpegXs` for RFC 9134 JPEG XS**), SdpSession (with `fromFile()` / `toFile()`, structured `RtpMap` / `FmtpParameters` accessors), MulticastManager, **MulticastReceiver**. PtpClock remaining.

### Phase 4: ProAV — MediaIO-Based Pipeline
**MediaIO framework complete.** Seven backends complete: `MediaIOTask_TPG` (test pattern generator, with `ImageDataEncoder` data stamp pass and AvSync LTC audio mode), `MediaIOTask_ImageFile` (DPX/Cineon/TGA/SGI/PNM/PNG/JPEG/RawYUV), `MediaIOTask_AudioFile`, `MediaIOTask_QuickTime`, `MediaIOTask_Converter`, the unified **`MediaIOTask_Rtp`** (writer + reader mode, MJPEG / L16 / JSON / JPEG XS payloads, SDP-driven auto-config, per-stream TX/RX threads, timing histograms), and the new **`MediaIOTask_Inspector`** (QA sink: image data band decode, audio LTC decode, A/V sync offset, continuity checks, periodic log, per-frame callbacks), plus `SDLPlayerTask` / `SDLPlayerOldTask` for display sink (with transparent compressed-video decode; see `proav_framesync.md` for the FrameSync-based vs legacy split). A **`JpegXsImageCodec`** using vendored SVT-JPEG-XS provides JPEG XS encode/decode for planar YUV 4:2:2/4:2:0 at 8/10/12-bit. The `mediaplay` utility exercises all backends end-to-end. Supporting utilities: `CRC<T>` template, `ImageDataEncoder`, `ImageDataDecoder`, format-agnostic `LtcDecoder::decode(const Audio &, channelIndex)` overload.

**Phase 4b additions (this session):** `EnumList` (ordered Variant-compatible list of runtime-typed enum values, DataStream round-trip); `InterlaceMode` TypedEnum replacing `ImageDesc`'s `bool interlaced`; `AudioTestPattern` rewrite with per-channel `EnumList<AudioPattern>` dispatch and seven new patterns (SrcProbe, ChannelId, WhiteNoise, PinkNoise, Chirp, DualTone, PcmMarker); `MediaIO::unknownConfigKeys()` + `validateConfigKeys()` with `ValidationMode::Lenient`/`Strict`; `MediaIO::Output`/`Input` semantics clarified (Output = source, Input = sink); `mediaplay` CLI renamed `-s`/`-d`/`--sc`/`--dc` + three-column config schema help layout.

**Phase 4c additions:** `MediaIOTask_ImageFile` sidecar audio — automatic Broadcast WAV sidecar alongside image sequences (write, read, seek, source hint); three new `MediaConfig` keys: `SidecarAudioEnabled` (bool, default true), `SidecarAudioPath` (String override), `AudioSource` (Enum `AudioSourceHint`); `AudioSourceHint` TypedEnum in `enums.h` (Sidecar/Embedded). Auto-sidecar `.imgseq` promoted to always-on-by-default with `SaveImgSeqEnabled` (bool, default true); sidecar filename auto-derived from sequence pattern when `SaveImgSeqPath` is empty (e.g. `shot_####.dpx` → `shot.imgseq`); mask-based reader auto-discovers the conventionally-named sidecar. `ImgSeq` gains an `audioFile` JSON field so the sidecar records the companion `.wav` filename. Sequence writer auto-creates the output directory (all missing parents). `String::truncated(maxChars)` utility added. `String::to<>()` / `toInt()` / `toUInt()` / `toDouble()` rewritten on `strtoll`/`strtoull`/`strtod`: proper `Error::Invalid` / `Error::OutOfRange` propagation, digit-group separator stripping (`_`, `,`, `'`), base-prefix detection (`0x`/`0b`/`0o`), `bool` delegation to `toBool()`. `MediaIOTask_Inspector` simplified log format and emits a structured final-report block on `close()`. Logger console formatter column order improved (timestamp and source before level).

**Phase 4e additions:** standalone `Clock` abstraction (`Clock`, `WallClock`, `SyntheticClock`) in core; `SDLAudioClock` replaces `SDLAudioPacerClock` (per-device `ClockDomain`, filtered `rateRatio()` for audio drift signalling); `FrameSync` resyncs a source stream to a destination clock's cadence via repeat/drop plus drift-corrected audio resampling, with selectable `DropOldest` / `Block` overflow policies; new `SDLPlayerTask` / `createSDLPlayer()` built on FrameSync displaces the FramePacer path, which is retained as `SDLPlayerOldTask` / `createSDLPlayerOld()` (deprecated); `MediaConfig::SdlPlayerImpl` chooses between them. See `proav_framesync.md`.

**Phase 4d additions (this session):** `MediaIOTask_Converter` no-drop write semantics — converter write path changed from hard `TryAgain` reject at capacity to a one-shot warning; frames are never silently dropped. `MediaIO::writeDepth()` / `writesAccepted()` API added: `writeDepth()` reflects the backend's preferred write pipeline depth (set via `MediaIOCommandOpen::defaultWriteDepth`); `writesAccepted()` computes available slots as `writeDepth() - pendingWrites() - task->pendingOutput()`, clamped to zero. `MediaIOTask::pendingOutput()` virtual added (default 0); `MediaIOTask_Converter` overrides it to return its output FIFO depth. `mediaplay` `Pipeline` back-pressure migrated from hardcoded `MaxInflightConverter`/`MaxInflightPerSink` counters to `writesAccepted()` queries on the downstream stage. Pipeline shutdown drain: source EOF with in-flight converter frames sets `_sourceAtEnd` and allows `drainConverter()` to flush remaining output to sinks before calling `finish(0)`. `WriteBeyondCapacity` test updated; `WritesAccepted` test added.

**Phase 4f additions:** `Enum` infrastructure overhaul — `PROMEKI_REGISTER_ENUM_TYPE` macro pins entry tables to rodata with three compile-time `static_assert` guards (non-empty, unique names/values, default present); `Enum::registerDefinition()` static-storage path added alongside the existing heap-allocating `registerType()`; `Enum::Definition` gains three parallel `StringLiteralData *` caches (`nameLit`, `entryNameLits`, `entryQualifiedLits`) so `typeName()`, `valueName()`, and in-list `toString()` return zero-copy literal-backed `String`s; two `Enum`s with the same in-list value share the same `cstr()` pointer enabling short-circuit `String ==`. `StringRegistry<Name>` migrated from phantom struct tag to `CompiledString` NTTP — `VariantDatabase<Name>`, `MediaConfig`, `Metadata`, `LibraryOptions`, `MediaIOStats`, `MediaIOParams`, `MediaIOStats`, `Config`, `ClockDomain`, `Benchmark` all updated. `PROMEKI_DECLARE_ID` macro introduced (replacing `static inline const ID = declareID(...)`) making well-known IDs `constexpr`-compatible. Phantom tag structs (`MediaConfigTag`, `LibraryOptionsTag`, `MediaIOStatsTag`, `MediaIOParamsTag`, `ConfigTag`) removed. `CompiledString` data members made public (C++20 structural type requirement for class-type NTTPs). `PROMEKI_INTENTIONAL_LEAK(ptr)` macro added to `util.h`; delegates to `__lsan_ignore_object()` under ASAN/LSAN, no-op otherwise. `etc/valgrind.supp` added with suppressions for the three intentional-leak call sites in `enum.cpp`. `stringregistry_constexpr.md` plan document removed (work complete). `CODING_STANDARDS.md` updated: well-known enum example switched to `PROMEKI_REGISTER_ENUM_TYPE`.

**Phase 4g additions:** `VideoCodec` + `AudioCodec` TypeRegistry infrastructure; `MediaPacket` shareable data object; `JpegVideoEncoder` / `JpegVideoDecoder` wrappers over `JpegImageCodec`; `JpegXsVideoEncoder` / `JpegXsVideoDecoder` wrappers over `JpegXsImageCodec`; `MediaIOTask_VideoEncoder` + `MediaIOTask_VideoDecoder` generic encoder/decoder MediaIO backends; `MediaIOTask_RawBitstream` elementary-stream file sink (h264/h265/hevc/bit extensions); CUDA utility (`include/promeki/cuda.h`); `NvencVideoEncoder` + `NvDecVideoDecoder` GPU-accelerated H.264/HEVC codec backends (gated behind `PROMEKI_ENABLE_NVENC` / `PROMEKI_ENABLE_NVDEC`; see `docs/nvenc.dox`); `typeregistry_isolation` test pinning cross-type registry isolation; `MediaConfig::CodecName` retired and replaced by typed `MediaConfig::VideoCodec` + `MediaConfig::AudioCodec`; codec string names switched to PascalCase (`H264`, `HEVC`, `JPEG`, `JPEG_XS`, `AAC`, `Opus`, `PCMI_S16LE`, etc.); `videodesc.h` / `videodesc.cpp` deleted (functionality fully covered by `MediaDesc`; basic `MediaDesc` tests migrated into `tests/mediadesc.cpp`).

**Phase 4h additions:** `H264Bitstream` / `AvcDecoderConfig` / `HevcDecoderConfig` NAL framing helpers (`include/promeki/h264bitstream.h`, `include/promeki/hevcbitstream.h`); QuickTime H.264/HEVC write support — Annex-B → AVCC rewrite, `avcC`/`hvcC` box extraction and emission, `appendStsdBox` shared helper, `parseVideoSampleEntry` with child-box walk; `QuickTime::Track::codecConfig()` / `codecConfigType()` accessors; `MediaIOTask_VideoEncoder` + `MediaIOTask_VideoDecoder` metadata FIFO fix (`_pendingSrcFrames`) correcting off-by-N timecode/audio after DPB reorder; `MediaIOTask_VideoEncoder` frame-rate propagation fix; `FrameSync` non-native audio conversion before resampler input; CSC SIMD fast paths for NV12↔RGBA8 and RGBA8↔NV12 plus new pixel-format pairs (NV21, semi-planar 4:2:2, RGB8↔NV12) with multi-width cross-validation; `Signal::connect(Function, ObjectBase*)` Qt AutoConnection overload; `Thread::id()` returning `std::thread::id`; `MediaIO` pipeline back-pressure fix (`frameWantedSignal` emitted after successful read).

**Phase 4i additions:** `VideoFormat` — new core class (raster + rate + scan triple) in `include/promeki/videoformat.h` / `src/core/videoformat.cpp`; Variant-registered as `TypeVideoFormat`; `std::formatter` specialization with `smpte`/`named` style keywords plus width/fill/align; `toString()` / `fromString()` (case-insensitive, tolerates whitespace and cosmetic separators, strict/loose interlaced field-rate modes); 49 `WellKnownFormat` entries and 11 `WellKnownRaster` entries, each with flag-based queries (`isSmpteFormat()`, `isNtscFormat()`, `isHdFormat()`, etc.); `allWellKnownFormats(flags)` filtered list accessor; comparison with `WellKnownFormat` enum values; full round-trip test suite. `VideoScanMode` TypedEnum in `enums.h` (Unknown / Progressive / Interlaced / InterlacedEvenFirst / InterlacedOddFirst / PsF), replacing `InterlaceMode` where applicable. `FrameRate::FPS_119_88` (120000/1001) and `FPS_47_95` (48000/1001) well-known entries added. `ImageDesc` gains a `videoScanMode()` field with DataStream serialization. `MediaDesc::videoFormat(index)` composition helper. `Frame::videoFormat(index)` and `Frame::mediaDesc()` assemblers. `Frame::makeString()` / `Image::makeString()` / `Audio::makeString()` — template string rendering with `@`-prefixed pseudo keys (image: `@Width`, `@Height`, `@Size`, `@PixelDesc`, `@PlaneCount`, `@LinePad`, `@LineAlign`, `@BytesPerPlane[N]`, `@ScanMode`, `@IsCompressed`, `@IsValid`; audio: `@SampleRate`, `@Channels`, `@DataType`, `@Samples`, `@MaxSamples`, `@Frames`, `@BytesPerSample`, `@IsValid`, `@IsCompressed`, `@IsNative`, `@CompressedSize`, `@CodecFourCC`; frame: `@ImageCount`, `@AudioCount`, `@HasBenchmark`, `@VideoFormat[N]`) plus `Image[N].key` / `Audio[N].key` subscript descent and user-supplied resolver callback. `VariantDatabase::format(tmpl, resolver, err)` — `{Key}` / `{Key:spec}` template engine with `{{`/`}}` escape, per-key `Variant::format(spec)` dispatch, optional fallback resolver for missing keys, `Error::IdNotFound` on any unresolved token. `VariantDatabase::setFromJson()` — spec-driven coercion of JSON strings back to the spec's native Variant type on deserialization. `Variant::format(spec)` — routes the spec to the held type's `std::formatter` specialization; falls back to `std::formatter<std::string_view>` for types without one; sets `Error::ConversionFailed` on bad spec. `MediaConfig::VideoFormat` ID added (TypeVideoFormat, default `VideoFormat()`). `VideoTestPattern` refactored to consume `VideoFormat` and `VideoScanMode` directly. `MediaIOTask_TPG` updated to use `VideoFormat` throughout. `VariantSpec` gains `parseString()` for spec-driven string→Variant coercion used by `setFromJson` and `fromJson`.

**Phase 4j additions:** NVENC pro-video features (HDR, VUI, timecode SEI, interlaced, multi-format, AV1) + QuickTime writer spec-cleanliness; compressed-essence pipeline refactor moving packets off `Frame::packetList` onto `Image::packet` / `Audio::packet`; new Simple types `ContentLightLevel` (CTA-861.3) and `MasteringDisplay` (SMPTE ST 2086) as Variant-registered types; `VideoRange` TypedEnum (`Unknown` / `Limited` / `Full`) + `PixelDesc::videoRange()` accessor auto-derived from `compSemantics[0]`; `ColorPrimaries`, `TransferCharacteristics`, `MatrixCoefficients` TypedEnums (ISO/IEC 23091-4 / ITU-T H.273); `VideoEncoderPreset::Lossless`; `VideoScanMode::isInterlaced()` predicate; `ColorModel::toH273()` library-level H.273 codepoint resolver; `PixelDesc::AV1` (ID 161), `YUV8_444_Planar_Rec709` (ID 162), `YUV10_444_Planar_LE_Rec709` (ID 163), `PixelFormat::P_444_3x10_LE` (ID 81); 12 new `MediaConfig` keys covering AQ, multi-pass, repeat-headers, timecode SEI, VUI color description, HDR metadata; 7 new `Metadata` keys (`ForceKeyframe`, `MasteringDisplay`, `ContentLightLevel`, `VideoColorPrimaries`, `VideoTransferCharacteristics`, `VideoMatrixCoefficients`, `VideoRange`); NVDEC bitstream→Metadata round-trip (VUI + HDR SEI + timecode SEI parsing via `pfnGetSEIMsg`); QuickTime writer: `ftyp` major brand, `sample_depends_on=2` on sync samples, `chan` atom in audio sample entry, per-fragment audio collapse, classic-as-default layout, LCM-based movie timescale; NVENC kernel-panic fixes (slot-resident HDR pointers for B-frame lookahead, H264-lossless-only-when-4:4:4, robust partial-init cleanup); `nvenc-functest` functional test binary with 92 pass cases; panic-resistant `fsync`-per-line logging; `Frame::@PacketCount` pseudo-key removed (packets now travel on `Image::packet` / `Audio::packet`, not a separate Frame list). See `devplan/nvenc.md` and `devplan/quicktime.md` for full detail.

**Phase 4k additions:** Optional async close for `MediaIO` and `MediaPipeline`, plus a graceful shutdown path in `mediaplay`. `MediaIO::close(bool block=true)` — non-blocking variant submits the backend `CmdClose` on the strand, pushes a synthetic `EndOfFile` read entry so signal-driven consumers see one clean EOS, resets cached descriptor state, and emits `closedSignal(Error)`; `isClosing()` gates new reads/writes while the finalize task is in flight. `MediaPipeline::close(bool block=true)` — cascading close: true source stages (no upstream) are closed first via `MediaIO::close(false)`; as each source's synthetic EOS latches `upstreamDone` in `drainSource`, every direct downstream consumer is closed in turn so intermediate stages see close only after their last input frame has been delivered; `finalizeClose()` emits `closedSignal` and the repurposed `finishedSignal` once every stage's `MediaIO::closedSignal` has fired; natural EOF auto-triggers the same cascade; dead `checkDrained`/`finish`/`_finished` removed. `Application::setQuitRequestHandler(QuitRequestHandler)` — installs a `std::function<bool(int)>` invoked from `Application::quit` before the default path; returning `true` suppresses `shouldQuit` / EventLoop-wake so an async shutdown can drive the eventual `Application::quit` itself. `mediaplay` wires these together: a quit handler posts `pipeline.close(false)` to the main EventLoop on first Ctrl-C / window-close / duration-timer / frame-count trigger; `pipeline.closedSignal` then calls `Application::quit`. 8 new `MediaIO` close tests, 7 new `MediaPipeline` cascade tests, 3 new `Application` quit-handler tests; 4054 unit tests pass.

**Phase 4l additions:** Cross-process frame transport (`FrameBridge`), EventLoop I/O source API, subsystem integration architecture, and IPC infrastructure. **EventLoop:** `addIoSource(fd, events, cb)` / `removeIoSource(handle)` — poll-based fd readiness notifications on the loop thread; thread-safe registration from any thread (wakes `poll()` to rebuild the fd set); implemented on top of `SelfPipe` (`pipe2` + `O_NONBLOCK` wake fd). `setWakeCallback()` removed in favour of the new IoSource model. `EventLoop::nextTimerTimeout()` made public for external pumps. `Application::exec()` added as thin wrapper over `mainEventLoop()->exec()`. **SignalHandler:** `subscribe(signo, cb)` / `unsubscribe(handle)` — routes non-termination signals (e.g. `SIGWINCH`) through the existing self-pipe watcher thread, dispatching callbacks in normal context; per-signal `sigaction` hook installed on first subscription. **Core IPC primitives:** `SelfPipe` (pipe2/O_NONBLOCK POSIX wake fd, drain/wake thread-safe); `SharedMemory` (POSIX `shm_open` with `create` / `open` / `close` / `unlink`, cross-user `chown`, move-only ownership semantics). **Network IPC:** `LocalSocket` (POSIX `AF_UNIX SOCK_STREAM`, derives from `IODevice`, connect/read/write/timeout); `LocalServer` (listen/accept/waitForNewConnection/nextPendingConnection). `KlvFrame` / `KlvReader` / `KlvWriter` — 4-byte FourCC + 4-byte big-endian length framing over any `IODevice`. **FrameBridge:** cross-process shared-memory frame transport using a seqlock ring of slots; output side creates shm + listens on a `LocalSocket` control channel; input side connects, performs `HELO`/`ACPT` KLV handshake, maps shm read-only, and receives `TICK` notifications; sync (ACK-gated) and async (free-running) consumer modes; `abort()` for pipeline shutdown; `writeFrame` / `readFrame` / `frameAvailable` signal / `framesMissed` signal. **MediaIOTask_FrameBridge:** MediaIO backend adapting `FrameBridge` for reader/writer use in `MediaPipeline` stages; 8 `MediaConfig` keys (`FrameBridgeName`, `FrameBridgeRingDepth`, `FrameBridgeMetadataReserveBytes`, `FrameBridgeAudioHeadroomFraction`, `FrameBridgeAccessMode`, `FrameBridgeGroupName`, `FrameBridgeSyncMode`, `FrameBridgeWaitForConsumer`); `Metadata::FrameBridgeTimeStamp` added for transport-latency measurement. **Subsystem architecture:** `SdlSubsystem` owns SDL init/quit + self-pipe + EventLoop IoSource bridge (SDL event watcher writes pipe, loop callback pumps SDL queue); `TuiSubsystem` owns terminal raw-mode, alternate screen, palette, input parser, STDIN IoSource, SIGWINCH subscription via `SignalHandler::subscribe`; both are stack-constructed after `Application`. TUI and SDL demos (`tui-demo`, `tui-event-test`) migrated to subsystem model. **LibraryOptions:** `IpcDir` key (String, default `/dev/shm/promeki` on Linux, `Dir::temp()` elsewhere); `Dir::ipc()` honours the override. **DataStream:** `TypeMasteringDisplay` (0x40) and `TypeContentLightLevel` (0x41) wire-format types with matching `operator<<`/`>>` and Variant round-trip; `Variant::TypeMasteringDisplay` and `Variant::TypeContentLightLevel` added. **Error:** `BrokenPipe` and `ConnectionReset` error codes added. Full test coverage: `eventloop.cpp` gains 7 IoSource tests; `signalhandler.cpp` gains 4 custom-subscription tests; `dir.cpp` gains 2 IpcDir tests; `datastream.cpp` gains 8 HDR round-trip tests; new `tests/unit/selfpipe.cpp`, `tests/unit/sharedmemory.cpp`, `tests/unit/network/klvframe.cpp`, `tests/unit/network/localsocket.cpp`, `tests/unit/framebridge.cpp`, `tests/unit/mediaiotask_framebridge.cpp`, `tests/unit/sdl/sdlsubsystem.cpp`, `tests/unit/tui/tuisubsystem.cpp`.

**Phase 4m additions:** `MediaPipelinePlanner` auto stage insertion, `MediaIODescription` introspection object, and `proposeInput` / `proposeOutput` negotiation API. **MediaIODescription:** new shareable value object capturing backend identity, role flags (`canBeSource` / `canBeSink` / `canBeTransform`), `producibleFormats` / `acceptableFormats` / `preferredFormat`, `canSeek` / `frameCount` / `frameRate` / `containerMetadata`, `probeStatus` / `probeMessage`; JSON + DataStream round-trip; `summary()` for human display. **Negotiation API:** `proposeInput(offered, preferred*)` and `proposeOutput(requested, achievable*)` synchronous virtuals on `MediaIOTask` (thin wrappers on `MediaIO`); three-way contract (accept as-is / want conversion / `NotSupported`); defaults pass through. **`FormatDesc::bridge` callback:** `bool(from, to, outConfig*, outCost*)` — registered per backend so the planner can query "can you convert from→to?" without touching the MediaIO; unitless cost bands (0 = identity, 1–10 = metadata, 10–100 = lossless, 100–1000 = lossy, 1000–10000 = heavy encode). **Planner:** `MediaPipelinePlanner::plan()` single-hop solver plus codec-transitive two-hop fallback (VideoDecoder→VideoEncoder via NV12 intermediate); four-strategy source-desc discovery (cached `mediaDesc()` → `describe().preferredFormat` → `expectedDesc()` → brief `open()/close()`); `InjectedStages` map overload for caller-built MediaIOs (SDL, device handles). **Refactors:** `MediaIO::Output`/`Input`/`InputAndOutput` renamed library-wide to `Source`/`Sink`/`Transform`; `FormatDesc::canOutput`/`canInput`/`canInputAndOutput` → `canBeSource`/`canBeSink`/`canBeTransform`; `setMediaDesc` → `setExpectedDesc` / `expectedDesc()`; `MediaIOTask_Converter` deleted (`feedback_csc_not_converter.md` updated); `MediaIOTask::applyOutputOverrides(input, config)` static helper. Bridge coverage: CSC, FrameSync, SRC, VideoDecoder, VideoEncoder, TPG, ImageFile, AudioFile, QuickTime, SDLPlayer. **`mediaplay` integration:** autoplan default, `--no-autoplan` opt-out, `--plan` prints resolved config, `--describe` prints `MediaIODescription::summary` per stage, SDL injection via `InjectedStages`. **Tests:** `mediaiodescription.cpp` (10 cases, 97 assertions), `mediaio_negotiation.cpp` (19 / 83), `mediaiotask_overrides.cpp` (15 / 35), `mediaio_bridges.cpp` (23 / 83), `mediapipelineplanner.cpp` (18 / 81). **Docs:** `docs/mediaplanner.dox` — algorithm, cost scale, `Policy` semantics, bridge authoring guide, worked examples. **Error:** `ParseFailed` error code added.

**Phase 4n additions:** `VariantLookup<T>` generic key-lookup registry, `VariantQuery<T>` compiled filter predicate, `DebugMediaFile` (PMDF) debug container, `MediaIOTask_DebugMedia` MediaIO backend, `pmdf-inspect` CLI utility, and capabilities-audit remediation for `RawBitstream` and `Burn`. **VariantLookup:** `PROMEKI_LOOKUP_REGISTER(T)` fluent builder registers named scalar / indexed-scalar / child / indexed-child / database-binding handlers per type; `resolve(T, key)` / `assign(T, key, value)` / `specFor(T, key)` / `format(T, tmpl)` static dispatchers with dotted-path and `[N]`-subscript descent; thread-safe static registry via per-type `ReadWriteLock`; introspection via `registeredScalars()` / `registeredIndexedScalars()` / `registeredChildren()` / `registeredIndexedChildren()` / `registeredDatabases()` / `forEachScalar()`. Registered for `Frame`, `Image`, and `Audio` — replaces the old `@`-pseudo-key `makeString()` API on all three. **VariantQuery<T>:** parses an expression once into an AST; evaluates against any `T` with a `VariantLookup<T>` registration; grammar: `==` / `!=` / `<` / `<=` / `>` / `>=` (with spec-driven string→native coercion) / `~` (regex) / `~~` (substring) / `has(key)` / `&&` / `||` / `!` with standard precedence; explicit instantiations for `Frame`, `Image`, `Audio`; `parse(expr)` returns `Result<VariantQuery<T>>`; `match(instance)` is reusable across many instances. **DebugMediaFile:** PMDF (ProMEKI Debug Frame) lossless debug container — 32-byte file signature + `SESN` session chunk + `FRAM`/`IMAG`/`AUDO` nested chunk structure via `DataStream`; `TOC` + `ENDF` footer written on clean `close()`; crash-resistant (truncated trailing FRAM triggers linear-scan index rebuild); `writeFrame` / `readFrame` / `readFrameAt` / `seek`; `sessionInfo()` carries `Metadata::SessionHostname`, `Metadata::SessionProcessId`, `Metadata::LibraryBuildInfo`, `Metadata::LibraryPlatform`, `Metadata::LibraryFeatures` auto-stamped on write. **MediaIOTask_DebugMedia:** MediaIO backend wrapping `DebugMediaFile`; registered as `"PMDF"` with `.pmdf` extension; `canBeSource` + `canBeSink`; `proposeInput` / `proposeOutput` declare any-input / any-output passthrough contract. **pmdf-inspect:** new utility (`utils/pmdf-inspect/`) — subcommands `summary`, `frame N`, `frames [--filter EXPR]`, `extract --from F --count C --into DIR`, `session`; delegates entirely to library primitives (`DebugMediaFile::index`, `Frame::dump`, `VariantQuery<Frame>::parse`, `MediaIO::copyFrames`). **FrameSync additions:** `FrameSyncDrop` + `FrameSyncRepeat` `Metadata` IDs stamped on every output frame; `pullFrame(blockOnEmpty)` overload for non-blocking MediaIO strand use. **RawBitstream remediation:** `describe()` advertises every registered compressed `PixelDesc` as acceptable; `proposeInput()` rejects uncompressed input with `Error::NotSupported` so the planner inserts a `VideoEncoder` rather than routing raw pixels at runtime. **Burn remediation:** `proposeInput()` rejects compressed and non-paintable input, substituting a same-family uncompressed `PixelDesc` via `MediaIOTask::defaultUncompressedPixelDesc()`; `proposeOutput()` declares passthrough. **MediaIOTask::defaultUncompressedPixelDesc(source):** shared helper (YCbCr sources → `YUV8_422_Rec709`, others → `RGBA8_sRGB`). **`MediaIO::copyFrames()`:** static utility — drains frames from an open source into an open sink with optional seek, count limit, and per-frame mutate callback; stops cleanly on EOF. **Burn template engine migrated** from `Frame::makeString()` to `VariantLookup<Frame>::format()`. **Tests:** `variantlookup.cpp`, `variantquery.cpp`, `debugmediafile.cpp`, `mediaiotask_debugmedia.cpp`, `mediaiotask_rawbitstream.cpp` (full coverage); `frame.cpp` adds `Image::dump`, `Audio::dump`, `Frame::dump` tests; `framesync.cpp` adds `FrameSyncDrop` / `FrameSyncRepeat` tests; `mediaio.cpp` adds `copyFrames` tests (6 cases). 4209 unit tests pass.

**Phase 4r additions:** `Url` class, `Variant::TypeUrl`, URL-based MediaIO factory, and URL scheme registration for `pmdf://` and `pmfb://`. **`Url`:** new Simple data object (`include/promeki/url.h`, `src/core/url.cpp`) — RFC 3986 parser and serializer supporting authority form (`scheme://[userinfo@]host[:port][/path][?query][#fragment]`) and opaque form (`scheme:opaque[?query][#fragment]`); scheme lowercased on parse; query exposed as ordered `Map<String, String>` with percent-decoded keys and values; builder API (`setScheme`, `setHost`, `setQueryValue`, etc.); `percentEncode` / `percentDecode` statics; `PROMEKI_FORMAT_VIA_TOSTRING` for `std::formatter` integration; full equality operator. **Variant integration:** `Variant::TypeUrl` (0x12 position in X-macro table) added to `variant.h` / `variant.tpp`; `From==String` conversion branch in `get<Url>()` parses via `Url::fromString`; `From==Url` → `String` emits `toString()`; `VariantSpec::singleTypeName` and `parseAsType` extended for `TypeUrl`. **DataStream:** `TypeUrl = 0x46` wire-format tag; `operator<<(const Url &)` serializes via `toString()` (string round-trip stable across library versions); `readVariantPayload` case for `TypeUrl` reconstructs via `Url::fromString`. **`MediaConfig::Url`** ID added (`Variant::TypeUrl`, default `Url()`): populated by `createFromUrl` so introspection consumers can recover the exact URL the caller passed. **MediaIO URL factory:** `MediaIO::createFromUrl(const Url &)` and string overload; `MediaIO::findFormatByScheme(scheme)` (case-insensitive walk of `FormatDesc::schemes`); `MediaIO::applyQueryToConfig(url, specs, outConfig)` — applies URL query parameters through `VariantSpec::parseString` + `validate`, fails fast on unknown keys, wrong-backend keys, coercion failures, or out-of-range values. URL takeover in `createForFileRead` / `createForFileWrite`: if the argument parses as a URL whose scheme is registered, routes to `createFromUrl` transparently. `FormatDesc` gains `StringList schemes` and `UrlToConfigFunc urlToConfig` fields. **`MediaIOTask_FrameBridge`:** registers scheme `"pmfb"`; `pmfb://<name>[?Key=value&...]` canonical form — authority is the bridge logical name (required), query parameters are the canonical `MediaConfig` key names handled by `applyQueryToConfig`; `formatDesc()` refactored from aggregate initializer to named-field assignment for clarity. **`MediaIOTask_DebugMedia`:** registers scheme `"pmdf"`; opaque form (`pmdf:/abs/path.pmdf`) and authority form with empty host (`pmdf:///abs/path.pmdf`) supported; non-empty host rejected; path component becomes `MediaConfig::Filename`. **`mediaplay --probe`:** updated to use URL-first dispatch — if the source argument parses as a URL with a registered scheme, `findFormatByScheme` + `urlToConfig` + `applyQueryToConfig` path is taken before falling back to the existing file-probe logic. **Tests:** `url.cpp` (new, 19 cases — invalid parse, authority/opaque forms, scheme lowercasing, query, percent encode/decode, round-trips, builder, equality, `removeQueryValue`); `variant.cpp` gains 6 cases (`Variant_Url_RoundTrip`, `Variant_Url_ToString`, `VariantSpec_Url_ParseString`, `VariantSpec_Url_ParseString_Rejects_Malformed`, `Variant_Url_FromStringVariant`, `Variant_Url_FromStringVariant_Malformed`); `datastream.cpp` gains 1 case (`DataStream: round-trip Variant Url`); `mediaiotask_debugmedia.cpp` gains 6 cases (scheme registration, opaque/authority URL factory, host rejection, empty-path rejection, `createForFileRead` URL passthrough); `mediaiotask_framebridge.cpp` gains 15 cases (scheme registration, name-only URL, all canonical query keys, empty-name rejection, malformed bool/int/unknown-key/non-backend-key rejection, `createForFileRead`/`createForFileWrite` transparency, unknown-scheme fall-through, full open via URL). 4406 unit tests pass.

**Phase 4q additions:** `FrameNumber`, `FrameCount`, and `MediaDuration` typed wrappers; migration of all raw `int64_t` frame indices and counts to these types. **`FrameNumber`** (`include/promeki/framenumber.h`, `src/core/framenumber.cpp`): absolute frame index; sentinel `Unknown` (-1); arithmetic poisons on Unknown; decrement at zero becomes Unknown; `std::hash` specialization. **`FrameCount`** (`include/promeki/framecount.h`, `src/core/framecount.cpp`): frame count with four states — Unknown (-1), Infinity (-2), Empty (0), and finite positive; cross-type arithmetic with `FrameNumber`; `toFrameNumber` / `toFrameCount` free-function conversions; `FrameCount::infinity()` and `FrameCount::unknown()` statics. **`MediaDuration`** (`include/promeki/mediaduration.h`, `src/core/mediaduration.cpp`): start `FrameNumber` plus `FrameCount` length; owns `FrameRange` (inclusive `[start..end]`) with forward iterator for range-for; `contains` / `overlaps` / `intersect` / `append` / `prepend` / `toFrameRange`. **DataStream:** `TypeFrameNumber` (0x43), `TypeFrameCount` (0x44), `TypeMediaDuration` (0x45) wire-format types; `Variant::TypeFrameNumber`, `TypeFrameCount`, `TypeMediaDuration` added. **Migration:** all `MediaIO`, `MediaPipeline`, `MediaPipelinePlanner`, `DebugMediaFile`, `MediaIOTask_*`, `FrameSync`, `ImageFile`, `AudioFile`, `Timecode::toFrameNumber()` return type changed to `FrameNumber`/`FrameCount`/`MediaDuration`; raw `int64_t` frame fields replaced throughout. **Error codes:** `DurationUnknown`, `FrameRangeInfinite`, `NotAdjacent` added. **Tests:** `framenumber.cpp` (9 cases), `framecount.cpp` (9 cases), `mediaduration.cpp` (20+ cases including `FrameRange` iteration); existing MediaIO / pipeline tests updated.

**Phase 4p additions:** Replace `FramePacer` with abstract `Clock` hierarchy; add `MediaIOClock` and `SDLPlayerWidget`. **New types:** `Clock` (abstract base — filter pipeline, fixed offset, pause accounting with three modes, monotonic clamp, `sleepUntil`, `Clock::Ptr` shared ownership); `WallClock` (`std::chrono::steady_clock`-backed); `SyntheticClock` (frame-count driven, `sleepUntilNs` is no-op for offline pipelines); `MediaIOClock` (synthesizes raw time from `MediaIO::currentFrame * framePeriod`, propagates `Error::ObjectGone` via `ObjectBasePtr` when owner is destroyed); `SDLPlayerWidget` (owns `SDLPlayerTask`/`MediaIO` pair, paints via `SDLVideoWidget`, routes key events for play/pause). **Reworked:** `SDLAudioClock` rewritten on new `Clock` base; `SDLAudioOutput` gains `createClock()`, `clockDomain()`, `setPaused()`, `setPlaybackRate()`; `FrameSync::setClock` takes `Clock::Ptr`; `SDLPlayerTask` replaced ad-hoc pacer with `FrameSync` + `Clock::Ptr`; `SdlSubsystem` gains periodic `SDL_PumpEvents` timer; `MediaConfig::SdlPlayerImpl` key removed. `SDLPlayerOld` deleted. `FramePacer` deleted. **Tests:** `clock.cpp` (14 cases — `Clock` base machinery, `FakeClock`), `mediaioclock.cpp` (8 cases), `framesync.cpp` (18 cases — steady-state, up/down-rate, audio resampling), `sdl/sdlaudioclock.cpp` (5 cases), `sdl/sdlsubsystem.cpp` (8 cases — lifecycle, multi-window, event loop integration).

**Phase 4o additions:** Compile-time infrastructure pass — `variant_fwd.h` forward-declaration header, `.tpp` body-separation files, `ObjectBasePtr<T>` class-template promotion, CMake build-tooling, and miscellaneous fixups. **Header-include cost reduction:** `variant_fwd.h` exposes forward declarations for `VariantImpl`, `Variant`, and `VariantList` so widely-included headers (`signal.h`, `slot.h`, `textstream.h`, `datastream.h`, `json.h`, `objectbase.h`) no longer pull `variant.h` (and its ~40 transitive headers) into every translation unit. **`.tpp` body-separation:** `variant.tpp` holds the `VariantImpl::get<T>()` visitor and `operator==` bodies; `variant.cpp` drives the matching `extern template` explicit instantiations via `PROMEKI_VARIANT_TYPES` X-macro so each consumer TU pays zero re-instantiation cost. `signal.tpp` / `slot.tpp` hold `Signal::pack` / `Slot::pack` / `Slot::exec(VariantList)` / `Slot::execFromVariantList` bodies — caller TUs that need cross-thread marshalling include `promeki/objectbase.tpp`, which pulls both in one shot. **`ObjectBasePtr<T>` class-template:** parameter `T` (default `ObjectBase`) types the `data()` return; CTAD deduction guide `ObjectBasePtr(Foo*) -> ObjectBasePtr<Foo>` for `ObjectBasePtr ptr(obj)` syntax; converting copy-constructor `ObjectBasePtr<U> → ObjectBasePtr<T>` where `U` derives from `T` for implicit upcast; `friend template<U>` grants `ObjectBase` access to all specializations; `objectbase.tpp` updated for typed `senderTracker`; three new `objectbase.cpp` tests (typed `data()`, CTAD deduction, derived-to-base conversion). **CMake build tooling:** `PROMEKI_BUILD_STATS` option (default ON) wraps every compile with `scripts/build-stats-wrapper.sh` (GNU `/usr/bin/time -f`) appending `RSS_KB\tWALL_S\tUSER_S\tSYS_S\tTARGET` to `build/build-stats.log`; `scripts/build-stats-report.sh` prints top-N by wall/RSS and totals; silently skipped if `/usr/bin/time -f` probe fails. `PROMEKI_USE_CCACHE` option (default ON) auto-detects `ccache` and chains it after the stats wrapper via `CCACHE_SLOPPINESS=pch_defines,time_macros` so PCH-consuming TUs remain cacheable. `PROMEKI_USE_PCH` option (default ON) adds a precompiled header over stable stdlib and promeki infrastructure headers (`<cstdint>` … `<pair.h>`) to `promeki` target, shaving repeated parse cost on incremental builds. `PROMEKI_BUILD_STATS` description added to `DevRelease` build-type comment (now uses `-g1` instead of `-g`). `BUILD_BYPRODUCTS` added to all six vendored `ExternalProject_Add` calls (zlib-ng, libjpeg-turbo, SVT-JPEG-XS, libspng, libsndfile, libsamplerate, freetype) to fix Ninja dependency tracking on fresh builds. `PROMEKI_NVENC_SDK_DIR` env-var pickup fixed: CMake `set(...CACHE)` only honours a default on first configure, so `$ENV{PROMEKI_NVENC_SDK_DIR}` is now pre-read into `_promeki_nvenc_sdk_dir_default`; `docs/nvenc.dox` updated to reflect the new env-var name (`PROMEKI_NVENC_SDK_DIR`) and `-D` override precedence. **Miscellaneous:** `signalhandler.h` gains `#include <promeki/eventloop.h>` to bring `EventLoop` into scope where needed; `mediapipeline.cpp` and `sdlsubsystem.cpp` gain direct `#include <promeki/eventloop.h>` so the include is explicit rather than transient; `datastream.h` adds explicit forward declarations for Variant-alternative types used only as `const T &` parameters on operator overloads (`UUID`, `UMID`, `DateTime`, `TimeStamp`, `FrameRate`, `VideoFormat`, `Timecode`, `Color`, `ColorModel`, `MemSpace`, `PixelFormat`, `PixelDesc`, `Enum`, `EnumList`, `StringList`) and adds direct includes for `Size2D`, `Rational`, `XYZColor`, `MasteringDisplay`, `ContentLightLevel` which were previously pulled in transitively through `variant.h`; `json.h` de-inlines `set(UUID)`, `setFromVariant`, `forEach`, `add(UUID)`, `addFromVariant`, `forEach` to `json.cpp` where `variant.h` is safely included. `benchmarkrunner.cpp`, `datastream.cpp`, `textstream.cpp` gain explicit `#include <promeki/variant.h>` to replace the former transitive path.

The legacy `MediaNode` / `MediaPipeline` / concrete `*Node` classes and their two dedicated utilities (`vidgen`, `testrender`) have been deleted. The new `MediaPipeline` class (see `proav_pipeline.md`) is the main remaining work in this phase.

Network optimization core prerequisites — batch `sendmmsg`, kernel pacing (`SO_MAX_PACING_RATE`), `SO_TXTIME` enable path, `PacketTransport` abstraction, `RtpSession` migration — are all shipped (see `proav_optimization.md`).  Remaining optimization work is the per-packet `SCM_TXTIME` deadline computation inside `MediaIOTask_Rtp` and a future DPDK transport backend.

DSP (audio filters, resampler, format converter) is deferred — it will land later as `MediaIOTask_Converter` subclasses. See `proav_dsp.md`.

### Phase 5: TUI Widget Completion
Mostly independent. See `tui.md`. Can be parallelized with Phase 3/4.

### Phase 6: Music Library Completion
Mostly independent. Work through `music_theory.md` before `music_midi.md`.

### Phase 7: Enhanced Existing Classes
Ongoing throughout other phases. String, Variant, and RegEx enhancements, plus ObjectBase saveState/loadState via DataStream. See `core_utilities.md` and `core_streams.md`.

---

## Coding Standards and Testing Requirements

**All work in this plan must follow the project's established coding standards and testing practices.** See `CODING_STANDARDS.md` at the project root for the full specification. Key requirements:

### Coding Standards

Every new class and every modification to an existing class must follow:

- **File headers**: Doxygen `@file` block with `@copyright Howard Logic. All rights reserved.` and `See LICENSE file in the project root folder for license information.` with trailing period.
- **Include guards**: `#pragma once`, never `#ifndef`.
- **Namespace**: All code in `PROMEKI_NAMESPACE_BEGIN` / `PROMEKI_NAMESPACE_END`.
- **Naming**: PascalCase classes, camelCase methods, `_underscore` private members, `set*()` setters, bare noun getters, `is*()` predicates, `to*()` conversions, `from*()` factories.
- **Indentation**: 8-space-wide tabs. `.clang-format` enforces formatting.
- **Object categories**: Simple data objects (no `PROMEKI_SHARED_FINAL`), Shareable data objects (`PROMEKI_SHARED_FINAL` + `::Ptr` + `::List` + `::PtrList`), Functional objects (derive from `ObjectBase`, use `PROMEKI_OBJECT`). No internal `SharedPtr<Data>` — ever.
- **Error handling**: Use `Error` class, never `bool` returns for error reporting. Preferred patterns: `Result<T>` (alias for `Pair<T, Error>`), direct `Error` return, or `Error *err = nullptr` output parameter.
- **Blocking calls**: Must accept `unsigned int timeoutMs = 0` (0 = wait indefinitely). Return `Error::Timeout` on expiry.
- **Public API**: No naked `std::` types in public interfaces — wrap in `using` aliases inside the class.
- **Doxygen**: All public classes and methods documented with `/** ... */` style. `@brief` required. `@param`, `@return`, `@tparam` as applicable.
- **No `using namespace std;`** in any file. Forward declare in headers where possible.

### Testing

Every new class must have complete unit tests. Every modification to an existing class must update its tests. No exceptions.

- **Framework**: doctest, vendored in `thirdparty/doctest/`.
- **File naming**: `tests/<classname>.cpp` matching the class being tested.
- **Structure**: `TEST_CASE("ClassName")` with `SUBCASE` for each behavior.
- **Coverage expectations**: Tests must cover default/invalid states, all public methods, edge cases, round-trip operations, operator overloads, copy/move semantics, error paths, and thread safety where applicable.
- **Assertions**: `CHECK()` for non-fatal, `REQUIRE()` only when subsequent checks depend on it. `doctest::Approx()` for floating-point.
- **Test executables**: `unittest-promeki` (consolidated), `unittest-tui`, `unittest-sdl`. Tests run automatically during build via CTest.
- **Build verification**: `build` command compiles and runs all tests. A class is not done until its tests pass.

---

## Cross-Platform Considerations

**WASM readiness:** All interfaces must be designed so they can work under WASM later:
- No blocking calls in public API without async alternatives
- IODevice and sockets use virtual methods for platform-specific backends
- ThreadPool degrades gracefully (thread count 0 runs tasks inline)
- Process class uses `#ifdef` platform guards
- FilePath/Dir designed for graceful degradation on Emscripten virtual FS

**Future SRT support:** Socket and pipeline interfaces designed to accommodate `SrtSocket` later without API changes. No SRT-specific abstractions now.

## Key Decisions

- **Network IO:** Raw POSIX sockets (no libuv/asio)
- **TLS:** Vendored mbedTLS
- **AV-over-IP:** General building blocks targeting open standards (ST 2110, AES67)
- **Pipeline unit:** `MediaIO` instance. No node wrapper. Conversions are Converter MediaIOs.
- **Pipeline topology:** Declarative config (stages + routes) → JSON-serializable → `MediaPipeline::build()` instantiates and wires
- **Threading:** Per-MediaIO `Strand` on a shared `ThreadPool`. Signal-driven frame movement, no dedicated pumper thread.
- **Containers:** Header-only templates following `List<T>` pattern
- **Concurrency:** Wrapping `std::` primitives with Qt-style API
- **Streams:** Separate DataStream (binary) and TextStream (formatted text). Both operate over IODevice or in-memory buffers, independent of `std::ostream`.

---

## Benchmark Infrastructure

See [benchmarking.md](benchmarking.md) for the full plan. **Shipped so far:** library-native `BenchmarkRunner` + `StatsAccumulator`, the unified `promeki-bench` utility (CSC suite only, programmatically generated from `PixelDesc::registeredIDs()`), MediaIO stamp hooks (enqueue / dequeue / taskBegin / taskEnd), the `MediaIO` identifier triple (`localId` / `Name` / `UUID`), three new `MediaConfig` keys (`Name`, `Uuid`, `EnableBenchmark`), Part D live telemetry (`RateTracker`, `BytesPerSecond`/`FramesPerSecond`/drop/repeat/late counters, latency keys, `PendingOperations` from `Strand::pendingCount()`, `MediaIOStats::toString()` compact log-line renderer, urgent `stats()` dispatch), and `mediaplay --stats` / `--stats-interval`. **Still pending:** the non-CSC microbench suites (network / codec / container / concurrency / variantdatabase / histogram), Part E MediaIO end-to-end cases (blocked on `MediaPipeline`), and CI regression integration.

---

## Doxygen Module Organization

### Doxygen groups — COMPLETE

All `@defgroup` definitions exist in `docs/modules.dox`: containers, concurrency, io, streams, strings, events, audio, media, math, time, util, crypto, network, proav, color, pipeline, paint, music, tui_widgets, tui_core. Color science guide at `docs/color.dox`. MediaIO authoring guide at `docs/mediaio.dox`.

### Remaining Doxygen work
- [ ] Add `@ingroup` tag to every new class's Doxygen comment
- [ ] Retrofit `@ingroup` tags onto existing classes as they are modified
- [ ] `docs/mediapipeline.dox` — authoring guide, JSON schema, worked examples (new)
- [ ] `docs/mediaplay.dox` — new CLI grammar reference
- [ ] Verify generated Doxygen output has navigable module tree

---

## Existing FIXMEs

16 tracked items in [fixme.md](fixme.md):

| File | Issue | Natural Phase |
|---|---|---|
| `src/core/file.cpp:40` | Windows File implementation is a stub | Phase 2 |
| `src/proav/audiogen.cpp:66` | Audio generation doesn't handle planar formats | Phase 4 |
| `src/core/datetime.cpp:112` | Should use `String::parseNumberWords()` instead of `strtoll` | Phase 7 |
| various | Replace direct std library usage with library wrappers | Phase 7 |
| `src/proav/mediaiotask_quicktime.cpp` | LE float audio storage is lossy (promoted to s16) | Phase 4 |
| `src/core/pixeldesc.cpp` | `raw ` BGR vs RGB byte-order disagreement | Phase 4 |
| `CMakeLists.txt` | SDL incremental-rebuild misses header ABI changes | Phase 4 |
| `include/promeki/bufferpool.h` | BufferPool available but not wired into QuickTime hot path | Phase 4 |
| `src/proav/quicktime_reader.cpp` | Fragmented reader ignores `trex` defaults fallback | Phase 4 |
| `src/proav/mediaiotask_quicktime.cpp` | Compressed audio pull-rate drifts (one packet/video frame) | Phase 4 |
| `src/proav/quicktime_reader.cpp` | Minimal XMP parser only matches `bext:` prefix (blocked on core XML support) | Phase 4 / Core |
| `src/proav/jpegxsimagecodec.cpp` | JPEG XS: SVT packed-RGB validation bug (workaround via CSC to planar) | Phase 4 |
| `src/proav/jpegxsimagecodec.cpp` | JPEG XS: additional matrix/range/colour-space variants | Phase 4 |
| `src/proav/quicktime_writer.cpp` | JPEG XS: QuickTime/ISO-BMFF container support (`jxsm` sample entry) | Phase 4 |
| `src/proav/mediaiotask_rtp.cpp` | JPEG XS: RFC 9134 RTP slice packetization + fmtp generation | Phase 4 |
| `src/proav/quicktime_writer.cpp` | Compressed audio write path missing (remux blocked) | Phase 4 |
