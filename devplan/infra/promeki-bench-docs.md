# Documentation for `promeki-bench`

`promeki-bench` (the unified microbench driver) ships as a binary
with `--help` and a CSC suite, but has no narrative documentation
yet.

## Tasks

- [ ] `docs/promeki-bench.dox` — usage, suite catalogue, parameter
  reference (`-p key=value` / `key+=value`), JSON output schema,
  baseline-comparison workflow.
- [ ] Man page (built into the install target).
- [ ] Link from `docs/utils.dox`.
- [ ] Cross-link from
  [infra/benchmarking.md](benchmarking.md) ("authoring guide" ToDo).
